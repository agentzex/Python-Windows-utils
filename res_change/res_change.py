from resolution_changer import revert_resolution, change_resolution


if __name__ == "__main__":

    # Use these functions to change the resolution of your monitor on Windows.
    # change_resolution - change the display resolution to a given width and height parameters
    # revert_resolution - reverts the display resolution back to its default parameters
    # example:
    width = 800
    height = 600
    #print change_resolution(width, height)
    print revert_resolution()